import React from 'react';
import Button from './button';

function Body() {
    return (
        <div>
            <h1> Click the button to toggle the light theme!</h1>
            <Button />
        </div>
    );
}

export default Body;